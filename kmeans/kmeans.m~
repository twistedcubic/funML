
% K means algorithm for clustering: 
% Initialize centroids
% Loop over: 1) classify all data to nearest centroids
% 2) Update centroids


% load data
X = load('data.txt');

% center and normalize data (along columns)
X = bsxfun(@mX - mean(X);
X = X - std(X);

% number of centroids
cent_num = 5;
% number of iterations
maxIter = 10;

% randomly pick initial centroids
m = size(X,1);
%pick cent_num random ints from 1 to m
cent_init = rand(      ) 

for i = 1:maxIter
% returns which data point belongs to which centroid (index)
cent_position = findCent(X, cent);

cent = updateCent(X, cent_init, cent_num);
end


