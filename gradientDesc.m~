function [theta, C] = gradientDesc(X, h, theta0, maxIter)
% computes theta that minimizes cost function h, using
% initial parameter theta0, up to maxIter iterations

% record cost for debugging: 
cost = zeros(maxIter, 1);
[cost0, G0]  = h(theta0, X*theta0);

for i = 1:maxIter
    h = 
end

end